# AGENTS.md — Nous 开发行为准则

> **取舍：** 本准则偏向「谨慎优先于速度」。对于琐碎任务，自行判断。

## 1. 先想后写

**不要假设。不要隐藏困惑。摆出取舍。**

实现之前：

- 写任何代码之前，必须先梳理清楚逻辑：目标、输入与输出、数据流、边界与异常、影响面。逻辑未理清不动笔。
- 明确说出你的假设。不确定就问。
- 若存在多种解释，把它们摆出来——不要默默选一个。
- 若有更简单的做法，说出来。该反驳时就反驳。
- 若有不清楚的地方，停下来。指明困惑点，然后提问。
- 考虑问题必须全面、周到、专业；逻辑性必须非常高。

## 2. 简单优先

**解决问题所需的最少代码。不做任何投机。**

- 不做要求之外的特性。
- 不为一次性代码造抽象；抽象必须由真实的重复或明确的边界驱动。
- 不做未被要求的「灵活性」或「可配置性」。
- 不为不可能发生的场景写错误处理。
- 若写了 200 行而它本可以只需50 行，重写。
- 自问：「资深工程师会不会说这太复杂了？」会，就简化。
- 代码质量与整洁度必须非常高。
- 禁止脏代码：重复逻辑、复制粘贴、超长函数、职责混杂、含糊命名、死代码、被注释掉的代码、滥用类型断言。
- 确需保留的 TODO 必须写明原因与归属。

## 3. 手术式修改

**只动你该动的。只清理你自己造成的烂摊子。**

编辑既有代码时：

- 不要「顺手改进」相邻的代码、注释或格式。
- 不要重构没坏的东西。
- 风格向现有代码看齐，哪怕你更喜欢另一种写法。
- 若发现无关的死代码，提一句——不要删。

当你的改动产生孤儿时：

- 删除因**你的**改动而不再被使用的 import / 变量 / 函数。
- 未被要求时，不要删既有的死代码。

检验标准：每一行改动都应能直接追溯到用户的需求。

- 全仓禁止补丁：不得以临时代码、绕过逻辑、调用点特判来掩盖根因，必须修复根因。



## 4. 目标驱动执行

**定义成功标准。循环直到验证通过。**

把任务转成可验证的目标：

- 「加校验」→「为非法输入写测试，然后让它们通过」
- 「修 bug」→「写一个能复现它的测试，然后让它通过」
- 「重构 X」→「确保重构前后测试都通过」

多步任务时，给出简短计划：

```
1. [步骤] → verify: [检查]
2. [步骤] → verify: [检查]
3. [步骤] → verify: [检查]
```

强成功标准让你能独立循环。弱标准（「让它能跑」）需要不断澄清。

## 5. 解决问题的思考方式

**不能只看局部问题，得思考根因，彻底解决同类问题。**


## 项目专属规则

- 技术栈必须新且高质量；优先现代、活跃维护、类型完备、生态成熟的方案。
- 注释必须规范，且一律使用中文：导出的函数、类、类型标注参数、返回值、异常；解释「为什么」与约束，不复述代码。
- 测试：所有测试集中在仓库根部唯一的 `tests/` 目录；按功能模块分类；每一类测试内部按等级分层。
- 文档：没有我的允许，禁止写文档。文档风格为：做什么/解决什么问题---怎么做。
- 临时产物：用完即清理，仓库始终保持整洁、可维护。

---

**这些准则起作用的标志是：** diff 中不必要的改动变少，因过度复杂化而返工的次数变少，澄清性的提问出现在实现之前，而不是错误之后。