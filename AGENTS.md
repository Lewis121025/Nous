# AGENTS.md — Nous 开发行为准则

> **取舍：** 本准则偏向「谨慎优先于速度，本质优先于表象」。非破坏性的只读任务或微小修复可快速响应，涉及状态机、数据流与公共接口的改动必须严格执行本准则。

## 1. 先想后写

**不要假设。不要隐藏困惑。摆出取舍。**

实现代码之前：

* **逻辑闭环：** 必须先理清逻辑闭环：输入与输出契约、核心状态流转、边界极限值、可能抛出的异常及下游调用影响面。逻辑未理清前不动笔。
* **假设显式化：** 不得携带未经验证的技术假设写代码。若需求存在模糊、冲突或多种实现分支，列出方案与取舍，停下来询问，禁止擅自替用户做推断。
* **敢于反驳：** 若既有方案或需求存在逻辑漏洞、过度设计或更优解，明确指出并给出替代方案，不盲目顺从。

## 2. 从根本出发，长期考虑

**从领域本质与数据流建模，彻底解决同类问题，为长期维护成本负责。**

* **第一性原理建模：** 遇到复杂逻辑或架构改动时，回归底层实体关系、数据流转与生命周期本身。优先通过理顺数据结构与核心状态机化解复杂度，禁止在腐化基础上叠加无意义的包装层或胶水代码。
* **根因治理（消灭同类问题）：**
* **严禁创可贴式补丁：** 严禁在调用端通过临时加 `if` 特判、空吞异常、绕过逻辑来掩盖问题。
* **溯源修复：** 必须顺着调用链路反推至破坏契约的第一发生点，从源头彻底修复，并横向排查全仓是否存留同类隐患。


* **长期主义不是过度设计：**
* 长期考虑体现在严谨的类型契约、清晰的职责边界与可测试的纯逻辑上。
* 严禁为虚构的未来场景编写无用的泛化抽象、预留插件或未要求的配置项。最易维护的代码永远是解决当下本质问题所需的最精炼代码。


* **零隐性技术债：**
* 严禁代码坏味道：重复逻辑、超长函数、大类、职责混杂、含糊命名、死代码及被注释的废弃代码。
* 严禁滥用类型断言（如 `any`、裸类型转换）掩盖类型系统错误。
* 确需保留的 TODO 必须标注原因与归属：`TODO(owner): [原因与移除条件]`。



## 3. 手术式修改

**只动你该动的。只清理你自己造成的烂摊子。**

编辑既有代码时：

* **禁止附带改动：** 严禁「顺手」格式化邻近代码、调整不相关代码缩进、修改历史注释或重构未坏的模块。
* **风格强跟随：** 遵循现有代码的编码范式、命名规范与工程约定，即使你更偏好另一种写法。
* **孤儿清理原则：** 仅清理因你本次改动而废弃的 import、临时变量、孤儿函数。对既有的历史遗留死代码，指出即可，不得擅自删除。
* **改动可追溯：** 提交的每一行代码改动，都必须能直接追溯到具体需求或 Bug 复现链路。

## 4. 目标驱动执行

**定义成功标准。以可验证的测试作为交付终点。**

把任务转成可运行验证的目标闭环：

* **Bug 修复：** 编写能够精准复现问题的测试 $\rightarrow$ 运行确认失败（红灯） $\rightarrow$ 实施源头修复 $\rightarrow$ 运行测试确认通过（绿灯） $\rightarrow$ 全局回归测试。
* **功能开发：** 明确输入输出断言 $\rightarrow$ 分步实现 $\rightarrow$ 每一步通过验证才推进下一步。
* **多步复杂任务时，给出简短计划模板：**
```text
1. [步骤 A] → verify: [可观测检查标准 / 执行命令]
2. [步骤 B] → verify: [可观测检查标准 / 执行命令]
3. [步骤 C] → verify: [完整回归测试]

```



---

## 项目专属规则

* **技术栈选型：** 技术栈必须新且高质量；优先采用现代、活跃维护、类型完备、生态成熟的方案。
* **注释与文档：**
* **一律使用中文注释：** 导出的函数、类、核心类型定义必须配齐规范中文注释，明确参数、返回值及可能抛出的异常。
* **解释「为什么」：** 注释重在阐述业务约束、前置条件与设计意图，严禁无意义复述代码字面逻辑。
* **严控外部文档：** 未经允许，严禁擅自新建或修改文档文件。获批后遵循固定结构：`做什么/解决什么问题 —— 怎么做`。


* **测试架构规范：**
* 所有测试集中在仓库根部唯一的 `tests/` 目录，禁止在业务源码目录中分散存放测试。
* `tests/` 内部按功能模块分类；每一类测试内部按等级分层（单元测试、集成测试、端到端测试职责隔离）。


* **临时产物管理：** 调试或测试过程中产生的一切临时脚本、日志与测试数据，用完即清理，保持工作区零污染。

---

**这些准则起作用的标志是：** diff 中不必要的改动变少，因过度设计或打补丁导致的返工变少，澄清性的提问出现在实现之前，而不是错误之后。